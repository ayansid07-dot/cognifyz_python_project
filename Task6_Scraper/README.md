---

```markdown
# Cognifyz Technologies - Internship
## Task 6: Interactive Web Scraper

### 📌 Overview
An automated web scraper built to fetch and parse quote data dynamically from a target website in a user-friendly format.

### 🛠️ Technologies Used
- Python 3.x
- `requests` library
- `BeautifulSoup` (`bs4`) library

### 🚀 Execution
Ensure dependencies are installed (`pip install requests beautifulsoup4`) and run the script:
```bash
python Task6_scraper.py